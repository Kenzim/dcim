[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = 
    -v
    --tb=short
    --strict-markers

# Coverage options (uncomment after installing pytest-cov: pip install pytest-cov)
# addopts = 
#     -v
#     --tb=short
#     --strict-markers
#     --cov=app
#     --cov-report=term-missing
#     --cov-report=html
#     --cov-report=xml

